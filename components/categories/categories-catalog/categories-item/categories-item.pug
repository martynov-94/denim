mixin categories-item(data)
    .categories-item__photo-wrapper
        img.categories-item__photo(src=data.photo.src, alt=data.photo.alt, width=data.photo.width, height=data.photo.height)
    span.categories-item__name= data.name